// src/types/genkit-ai__next.d.ts
// Intentionally export nothing to prevent any type interference.
// This module should not contribute any Next.js specific types.
declare module '@genkit-ai/next' {
  // By leaving this empty, we prevent any of its types from being resolved
  // and potentially conflicting with Next.js's own type definitions.
}

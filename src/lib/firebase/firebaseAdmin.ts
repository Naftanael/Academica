// src/lib/firebase/firebaseAdmin.ts
/**
 * A inicialização do Firebase foi temporariamente desativada para permitir que o projeto seja compilado.
 * A funcionalidade que depende do Firebase não funcionará.
 * A causa raiz dos problemas de compilação precisa ser investigada.
 */

export const db = {} as any; // Mock para evitar erros de importação.
export const messaging = {} as any; // Mock para evitar erros de importação.
